export const settings = {
  apikey: "place your api key here",
  secret: "place your secret here",
  coinList: ["COIN"],
  threshold: 1,
};
// coinList: to add more than one coin, add a comma after the last " , like this: "BTC", "ETH", "XRP"
// threshold: the amount of coin you want to sell, ej. 0.0001 (Only the number)
